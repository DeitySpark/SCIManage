export function editorClick() {
    this.isDisabled = false
    this.isShow = {
        save: true,
        view: false,
        edit: false,
        delete: true
    }
    console.log(isDisabled)
    console.log(isShow)
}
